<Window x:Class="FinanceTracker.Graphics.Dialogs.AddCategoryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FinanceTracker.Graphics.Dialogs"
        mc:Ignorable="d"
        Title="Přidat kategorii" Height="160" Width="300">
    <Grid Style="{StaticResource GradientBackgroundStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label Content="Název kategorie:" Style="{StaticResource WindowsLabelStyle}"  Grid.Row="0"/>
        <TextBox x:Name="CategoryNameTextBox" Style="{StaticResource WindowsTextBoxStyle}" Grid.Row="1" Margin="15,5,15,5" Width="NaN" HorizontalAlignment="Stretch"/>
        <Button Style="{StaticResource WindowsButtonStyle}" Grid.Row="2" Width="110" HorizontalAlignment="Left" Content="Přidat" Click="AddButton_Click"/>
        <Button Style="{StaticResource WindowsButtonStyle}" Grid.Row="2" Width="110" HorizontalAlignment="Right" Content="Zrušit" Click="CancelButton_Click" Margin="0,5,15,5"/>
    </Grid>
</Window>
